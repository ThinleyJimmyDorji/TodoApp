import type { Tree } from '@nrwl/devkit';
export interface ComponentInfo {
    componentFileName: string;
    moduleFolderPath: string;
    name: string;
    path: string;
}
export declare function getComponentsInfo(tree: Tree, moduleFilePaths: string[], projectName: string): ComponentInfo[];
